A small project keeps its styles close to its content.

```
project/
├── index.html
└── styles/
    └── main.css
```

## Keep it organized

- One file per major section as the site grows
- Comments with `/* section name */` markers
- Consistent naming for classes across the project
