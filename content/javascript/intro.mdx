JavaScript is the only language that runs natively in every browser. It powers interactivity on the web and, through Node.js, the server too.

## What you will learn

- Values, types and coercion
- Functions and the call stack
- Scope, closures and `this`
- The async model: promises and `await`
